# 基础配置：网络 / 串口 / 上电策略 / 场景状态 / DI 上传驱动（所有固件变体共用）

substitutions:
  name: myvan-485-gateway
  friendly_name: 485网关

esphome:
  name: ${name}
  name_add_mac_suffix: false
  friendly_name: ${friendly_name}
  on_boot:
    - priority: -100
      then:
        - lambda: 'gpio_set_direction(GPIO_NUM_5, GPIO_MODE_OUTPUT);'
        - delay: 3s          # 等模块上电就绪
        # 三个模块输入主动上传开启（保持寄存器 0031H=1；掉电保存状态不确定，每次上电写一次）
        - lambda: 'gpio_set_level(GPIO_NUM_5, 1);'
        - delay: 1ms
        - uart.write:
            id: rs485_bus
            data: [0x01, 0x06, 0x00, 0x31, 0x00, 0x01, 0x19, 0xC5]
        - lambda: 'id(rs485_bus).flush();'
        - lambda: 'gpio_set_level(GPIO_NUM_5, 0);'
        - delay: 10ms
        - lambda: 'gpio_set_level(GPIO_NUM_5, 1);'
        - delay: 1ms
        - uart.write:
            id: rs485_bus
            data: [0x02, 0x06, 0x00, 0x31, 0x00, 0x01, 0x19, 0xF6]
        - lambda: 'id(rs485_bus).flush();'
        - lambda: 'gpio_set_level(GPIO_NUM_5, 0);'
        - delay: 10ms
        - lambda: 'gpio_set_level(GPIO_NUM_5, 1);'
        - delay: 1ms
        - uart.write:
            id: rs485_bus
            data: [0x03, 0x06, 0x00, 0x31, 0x00, 0x01, 0x18, 0x27]
        - lambda: 'id(rs485_bus).flush();'
        - lambda: 'gpio_set_level(GPIO_NUM_5, 0);'
        - delay: 10ms
        # 安全负载：每次上电强制关（覆盖 restore 恢复值）
        - switch.turn_off: sw_spot
        - switch.turn_off: sw_outdoor
        - switch.turn_off: sw_dsp
        - switch.turn_off: sw_fan_pass
        - switch.turn_off: sw_fan_rear
        - switch.turn_off: sw_pump
        - switch.turn_off: sw_valve
        # LED DO：强制灭，与场景 globals 初始值（全关/正常）保持一致
        - switch.turn_off: sw_led_outdoor
        - switch.turn_off: sw_led_camp_green
        - switch.turn_off: sw_led_camp_red
        - switch.turn_off: sw_led_scene_red
        - switch.turn_off: sw_led_scene_green
        - switch.turn_off: sw_led_fridge

api:
  encryption:
    key: "BTGcLeExk4xanEi/1zwV6fVzQJiULFGZDbkXac4eFCw="

ota:

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: arduino

web_server:
  port: 80

wifi:
  networks:
    - ssid: "ooxx"            # 家里调试（占位符，改成真实家庭 wifi）
      password: www.baidu.com
    # - ssid: "my-van"         # 车上网络（路由/热点方案定后启用，优先级 2）
    #   password: ""
  use_address: 192.168.5.18
  ap:
    ssid: "myvan-ap"          # 兜底 AP（调试用），与车上热点名区分
    password: "12345678"
captive_portal:

logger:
  level: INFO

uart:
  id: rs485_bus
  tx_pin: GPIO15
  rx_pin: GPIO16
  baud_rate: 38400

# ============ DI 按键：模块主动上传驱动 ============
# 模块输入变化即发上传帧（<地址> 04 02 <16位位图> <CRC>），零轮询、总线空闲时无流量
# 100ms 高频读缓冲，只解析上传帧并按模块地址分发到对应按键
interval:
  - interval: 100ms
    then:
      - script.execute: read_upload

script:
  - id: read_upload
    mode: single
    then:
      - lambda: |-
          uint8_t buf[64];
          int len = 0;
          while (id(rs485_bus).available() && len < 64) {
            uint8_t b;
            if (id(rs485_bus).read_byte(&b)) buf[len++] = b;
          }
          // 上传帧: <模块地址> 04 02 <16位位图> <CRC低> <CRC高>，位图 bit0=X1 ... bit7=X8，按下/释放各一次
          for (int i = 0; i + 6 < len; i++) {
            if (buf[i+1] == 0x04 && buf[i+2] == 0x02) {
              uint16_t crc = 0xFFFF;
              for (int k = i; k < i + 5; k++) {
                crc ^= buf[k];
                for (int bit = 0; bit < 8; bit++) {
                  if (crc & 1) crc = (crc >> 1) ^ 0xA001;
                  else crc >>= 1;
                }
              }
              if ((uint8_t) crc == buf[i+5] && (uint8_t) (crc >> 8) == buf[i+6]) {
                uint16_t r = ((uint16_t) buf[i+3] << 8) | buf[i+4];
                ESP_LOGI("di", "keys addr=%02X: %04X", buf[i], r);
                switch (buf[i]) {
                  case 0x01:  // 额头
                    id(bs_cabin_spot).publish_state(r & 0x01);
                    id(bs_cabin_reverse).publish_state(r & 0x02);
                    id(bs_cabin_dsp).publish_state(r & 0x04);
                    id(bs_cabin_fan).publish_state(r & 0x08);
                    id(bs_cabin_scene).publish_state(r & 0x20);
                    break;
                  case 0x02:  // 中8
                    id(bs_door_drain).publish_state(r & 0x01);
                    id(bs_door_outdoor).publish_state(r & 0x02);
                    id(bs_door_pump).publish_state(r & 0x04);
                    id(bs_door_camp).publish_state(r & 0x08);
                    id(bs_bed_light).publish_state(r & 0x10);
                    id(bs_bed_ambient).publish_state(r & 0x20);
                    id(bs_bed_fan).publish_state(r & 0x40);
                    id(bs_bed_pump).publish_state(r & 0x80);
                    break;
                  case 0x03:  // 中4
                    id(bs_bed_fridge).publish_state(r & 0x01);
                    id(bs_bed_scene).publish_state(r & 0x02);
                    break;
                }
                break;
              }
            }
          }

# ============ 场景状态（重启后回到初始值，与 LED 初始态一致） ============
globals:
  - id: light_scene          # 灯光模式: 0=全关 1=明亮 2=夜灯
    type: int
    initial_value: '0'
  - id: camp_scene           # 离车/驻车: 0=正常 1=离车 2=驻车
    type: int
    initial_value: '0'
  - id: fan_scene            # 排气扇: 0=全关 1=仅后窗 2=全开
    type: int
    initial_value: '0'
  - id: hold_triggered       # 主控键1 长按标记
    type: bool
    initial_value: 'false'
