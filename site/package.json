{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vitepress dev",
    "prebuild": "cd modules && rm -rf 0* 1* && rsync -a --exclude '.esphome/' --exclude 'secrets.yaml' ../../modules/0* ../../modules/1* ./ && find . -name README.md -exec sh -c 'mv \"$1\" \"${1%README.md}index.md\"' _ {} \\;",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
